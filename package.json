{
  "name": "chinese-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "A simplified Mandarin trainer for listening, writing, and reading practice.",
  "homepage": "https://ha-san-ali.github.io/chinese-trainer/",
  "scripts": {
    "check": "npm run check:syntax && npm run check:data",
    "check:data": "node scripts/validate-data.js",
    "check:syntax": "node --check app.js && node --check sentence-data.js && node --check scripts/validate-data.js && node --check scripts/build-site.js",
    "build": "node scripts/build-site.js"
  },
  "engines": {
    "node": ">=20"
  }
}
